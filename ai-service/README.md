AI Service (FastAPI)

Commands

- Run locally: pip install -r requirements.txt && uvicorn app.main:app --reload --port 8000
- Docker: docker build -t novarecover-ai ./ai-service && docker run -p 8000:8000 novarecover-ai
